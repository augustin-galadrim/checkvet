components: []
container:
  properties:
    html: |-
      <style>
        .startup-container {
          display: flex;
          flex-direction: column;
          align-items: center;
          justify-content: center;
          padding: 40px 20px;
          gap: 15px;
          color: #888;
          font-style: italic;
          margin: 3em;
        }
        .spinner {
          width: 50px;
          height: 50px;
          border: 5px solid #f3f3f3; /* Light grey base */
          border-top: 5px solid #4CAF50; /* Checkvet Green */
          border-radius: 50%;
          animation: spin 1s linear infinite;
        }
        .status-text {
          font-size: 16px;
          font-weight: 500;
        }
        @keyframes spin {
          0% { transform: rotate(0deg); }
          100% { transform: rotate(360deg); }
        }
      </style>

      <div class="startup-container">
        <div class="spinner"></div>
        <p class="status-text" id="startupForm-center-loadingMessage">
          Loading application...
        </p>
      </div>

      <div anvil-slot="default"></div>

      <script>
      </script>
  type: HtmlTemplate
is_package: true
